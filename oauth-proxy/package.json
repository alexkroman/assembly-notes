{
  "name": "oauth-proxy",
  "version": "1.0.0",
  "description": "OAuth proxy for Slack integration",
  "type": "module",
  "main": "api/slack-oauth.js",
  "scripts": {
    "build": "echo 'No build required'",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@vercel/node": "^5.3.11"
  }
}
